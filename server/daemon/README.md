# server/daemon

## Overview

Control logic for starting and supervising the Phantom daemon. Responsible for graceful shutdown and lifecycle hooks.

## Go Files

- `daemon.go` – Starts the server daemon, manages signals, and orchestrates shutdown.
